# requirements.txt for FarmLink Backend

# Core Flask & extensions
Flask==3.1.2
flask-cors==6.0.2
Flask-Mail==0.10.0                  # ← You can keep this for local/dev testing if wanted
Flask-SQLAlchemy==3.1.1
flask-jwt-extended>=4.6.0
psycopg2-binary>=2.9.9 

# Database & MySQL driver
SQLAlchemy==2.0.46
PyMySQL==1.1.2

# Serial communication (for sensors)
pyserial==3.5

# Production server (Render, Railway, etc.)
gunicorn==25.1.0

# Utilities
blinker==1.9.0
click==8.3.1
colorama==0.4.6
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
packaging==26.0
python-dotenv==1.2.1
typing_extensions==4.15.0
Werkzeug==3.1.5

# ─── Email sending via Resend API (replaces blocked SMTP) ───────────────────
resend>=2.0.0                       # Official Resend Python SDK – latest is ~2.22 as of Feb 2026

# Optional: if you want stricter version pinning
# resend==2.22.0