# requirements.txt for FarmLink Backend
# Cleaned version - March 2026

# Core Flask & extensions
Flask==3.1.2
flask-cors==6.0.2
Flask-Mail==0.10.0
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.7.1
Flask-Migrate==4.1.0

# Database drivers (PostgreSQL + MySQL fallback)
psycopg2-binary>=2.9.9
PyMySQL==1.1.2
mysqlclient==2.2.4

# Production server
gunicorn==25.1.0

# Utilities & dependencies
blinker==1.9.0
click==8.3.1
colorama==0.4.6
greenlet==3.3.1
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
packaging==26.0
PyJWT==2.11.0
python-dotenv==1.2.1
SQLAlchemy==2.0.46
typing_extensions==4.15.0
Werkzeug==3.1.5

# Serial communication (for LoRa/sensors)
pyserial==3.5

# Reporting / PDF generation
reportlab==4.4.10

# Optional extras (if you're using charts or image processing server-side)
matplotlib==3.10.8
pillow==12.1.1

# Optional email alternative (if using Resend instead of Flask-Mail)
resend>=2.0.0